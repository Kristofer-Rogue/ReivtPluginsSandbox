<Window x:Class="PropertiesShower.Views.PropertiesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PropertiesShower.Views"
        mc:Ignorable="d"
        Title="PropertiesView" Height="800" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Frame Grid.Row="0" Content="{Binding PropertiesPage }" Margin="5"/>
        <Button Grid.Row="1" Content="Выбрать другой элемент" Width="200" Padding="5" Margin="5" Command="{Binding SelectAnotherElementCommand}"/>
    </Grid>
</Window>
